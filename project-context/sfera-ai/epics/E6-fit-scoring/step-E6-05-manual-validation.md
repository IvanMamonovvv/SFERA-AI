# Шаг E6-05 — ручной прогон и сверка с HR

**Статус:** DONE
**Слой:** QA · **Зависит от:** E6-04

## Цель

10–20 реальных кандидатов прогнаны через Fit scoring, HR подтверждает адекватность
скоров/рекомендаций (не автоматическая метрика — субъективная сверка владельцем/HR).

## Что сделать

1. Выбрать 10–20 кандидатов с разной полнотой данных (`data_completeness` разный) на
   одном `course` из пилота E6-04.
2. Прогнать реальный Fit-вызов, собрать результаты в читаемом виде (CLI-вывод или
   CSV) для сверки HR.
3. Зафиксировать расхождения (если есть) — отдельный документ или запись в `04_STATE.md`
   журнале, не блокирует эпик, если расхождения объяснимы.

## Файлы

- `src/sfera_ai/cli/run_fit_scoring_sample.py` — ручной прогон
- нет автотеста — это ручная валидационная сессия

## Критерии готовности (DoD)

- [x] 10–20 прогонов выполнены, результаты показаны HR/владельцу
- [x] Явное подтверждение «адекватно» получено (или список найденных проблем передан
      на доработку промпта — тогда шаг не закрывается)

## Как проверить

```bash
uv run python -m sfera_ai.cli.run_fit_scoring_sample --course-id <id> --limit 20
```

## Как отметить выполнение

1. Впиши результат в журнал ниже. 2. Статус → `DONE`. 3. Обнови `04_STATE.md` (эпик E6
   → DONE, следующий — E7 или E8 по графу зависимостей).

## Журнал

- `2026-08-27` — старт: `course_id=25` («Менеджер по продажам (ШКМ)», URL
  `sphera-link.online/candidates?course=fb16645fbe35418ea3342734becc489b`), `VacancyProfile`
  id=3 v1 создан (черновик требований, `notes` помечены «требует правки после сверки с
  HR» — не финальный текст, владелец согласился прогонять и дорабатывать по ходу).
  Реальный прогон на 7 из 167 кандидатов курса (facts + fit-scoring, см. журнал
  `step-E6-04-queue-integration.md` — те же вызовы закрыли и п.0 того шага). Результат:
  1 `WEAK_MATCH` (id=115, fit_score=40), 1 `POSSIBLE_MATCH` (id=126, fit_score=70), 5×
  `NOT_ENOUGH_DATA` (fit_score=null). **Шаг не DONE**: (1) DoD требует 10-20 прогонов, пока
  7; (2) явного подтверждения «адекватно» от HR/владельца ещё не было — доля
  `NOT_ENOUGH_DATA` (5/7) на `PARTIAL`-фактах пока не обсуждена — не факт, что это
  проблема кода/промпта, а не реального недостатка данных у этих кандидатов, но требует
  сверки. Дальше: либо расширить выборку до 10-20 на этом же course_id и уже на полном
  наборе показать владельцу, либо сначала итерация над `_SYSTEM_PROMPT` (fit_scoring.py)/
  экстракцией фактов (candidate_facts.py) — решение владельца, per план «сначала
  реализуем всё, потом обкатываем промпт».
- `2026-08-28` — выборка расширена до 20 (докатили ещё 13 кандидатов курса 25:
  сборка фактов + fit-scoring, реальные LLM-вызовы). Диагностика причины
  `NOT_ENOUGH_DATA`: сверены сырые `facts` — у всех кандидатов с этим вердиктом всего
  3 факта (`full_name`/`phone`/`city`, первые вопросы анкеты), про опыт продаж данных
  нет вообще — анкета не дозаполнена самим кандидатом, это не баг экстракции/промпта.
  У кандидатов с 8+ фактами модель давала осмысленный разброс: `POSSIBLE_MATCH` (5,
  fit 70-80), `WEAK_MATCH` (1, fit 40), `NOT_A_MATCH` (1, fit 10). Итог по 20: 12×
  `NOT_ENOUGH_DATA`, 5× `POSSIBLE_MATCH`, 1× `WEAK_MATCH`, 1× `NOT_A_MATCH` — чёткая
  корреляция с полнотой анкеты, промпт править не потребовалось. Полный CSV передан
  владельцу для сверки (вне репозитория — scratchpad, реальные ФИО/контакты кандидатов,
  не коммитить). DoD «10-20 прогонов» закрыт количественно. **Шаг всё ещё не DONE** —
  ждём явного «адекватно» от владельца/HR по существу оценок (не только по факту, что
  код не упал).
- `2026-08-28` — владелец подтвердил: (1) черновик требований к вакансии «пока
  устраивает» (`VacancyProfile` id=3 v1, `course_id=25` — не финальная, но принята для
  продолжения), (2) вердикты по 20 кандидатам — «да» (адекватно). Оба DoD-пункта
  закрыты. Шаг **DONE**. `step-E6-04-queue-integration.md` п.0 закрыт тем же прогоном
  (журнал того шага) — п.2 (снятие `ai_processing_dry_run` для пилота через очередь)
  отдельным решением ещё не выполнен, эпик E6 закрывается по завершении E6-05 (сама
  очередь — E6-04 — это включение автоматизации, не блокирует ручную QA-сверку).
