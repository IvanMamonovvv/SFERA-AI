# Шаг E16-03 — docs/DEPLOYMENT.md

**Статус:** DONE
**Слой:** Docs · **Зависит от:** E16-01, E16-02
**Перед началом:** прочитай `docs/LOCAL_DEV.md` (соседний документ, SSH/туннели — не
дублировать оттуда).

## Цель

Один файл, отвечающий «как поднять сервис на VPS с нуля» и «как обновить» — без
необходимости расспрашивать в чате каждый раз, какие env нужны и в каком порядке жать
кнопки.

## Что сделать

1. `docs/DEPLOYMENT.md` — состав сервиса (`ai-service`/`ai-scheduler`), первый деплой,
   day-2 обновление через `deploy-ai-service.sh`, полный список env-переменных (свои +
   парные в `sfera_backend`), явное правило «единственный инстанс планировщика» (E16-01) с
   обоснованием через существующие БД-гарантии (SKIP LOCKED/уникальный индекс).
2. Не дублировать содержимое `03_TDD.md`/`docs/LOCAL_DEV.md` — ссылаться.

## Файлы

- `docs/DEPLOYMENT.md` — новый.

## Критерии готовности (DoD)

- [x] Есть раздел «Первый деплой» и «Обновление» — разные последовательности действий.
- [x] Есть полный список env обоих репозиториев (SFERA-AI + парные в `sfera_backend`) в
  одном месте.
- [x] Явно расписано, почему нельзя scale-ить `ai-scheduler`.

## Как проверить

Прочитать `docs/DEPLOYMENT.md` — владелец (не профессиональный разработчик) должен суметь
пройти по шагам без дополнительных вопросов о том, что такое `.env`/зачем два контейнера.

## Журнал

- `2026-09-09` — выполнено. Файл ссылается на `03_TDD.md` («Инфраструктура и сеть») и
  `docs/LOCAL_DEV.md` (SSH/туннели/`ai_readonly`) вместо дублирования — «один факт — один
  файл» (правило `CLAUDE.md`).
