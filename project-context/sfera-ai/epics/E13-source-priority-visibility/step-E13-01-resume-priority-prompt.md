# Шаг E13-01 — Приоритет резюме в промпте fit-scoring

**Статус:** DONE
**Слой:** Backend (fit-scoring) · **Зависит от:** E6-01 (`candidate_facts.py`,
факты уже несут `evidence[0].source_type`), E6-03 (`fit_scoring.py`)
**Перед началом:** прочитай `src/sfera_ai/services/fit_scoring.py` (`_SYSTEM_PROMPT`,
`_build_messages`), `src/sfera_ai/services/candidate_facts.py` (как факты помечаются
источником — `evidence`), [[project_source_priority_resume_answers_video]] (решение
владельца 2026-09-01).

## Контекст

Владелец 2026-09-01 сформулировал: резюме — важнейший источник данных о кандидате
(связь с кандидатом + опыт), 2-й по значимости — ответы на анкету, 3-й — видеовизитка.
При противоречии между источниками по одному и тому же параметру (например разный
опыт работы в резюме и в ответе) — верить резюме, остальные источники не отбрасывать,
использовать как дополнение там, где в резюме этого параметра нет. Явно уточнено:
это НЕ hard-block и НЕ блокировка кандидата — при малом объёме данных оценка должна
органически быть ниже/осторожнее, не через искусственный потолок.

Сейчас `_build_messages` (`fit_scoring.py`) передаёт LLM плоский список
`candidate_profile.facts` без указания на приоритет между источниками — модель не
знает, что резюме важнее анкеты при конфликте.

## Цель

При противоречии между источниками ИИ в оценке кандидата отдаёт приоритет резюме;
факты из ответов/видео используются как дополнение, не отбрасываются.

## Что сделать

1. Проверить, что каждый факт в `profile.facts` действительно несёт
   `evidence[0].source_type` (`HH_RESUME`/`ANKETA_FILE`/`ANSWER`/`VIDEO`) — уже так
   устроено в `_resume_facts`/`_answers_facts`/`_video_facts` (`candidate_facts.py`).
2. Дополнить `_SYSTEM_PROMPT` в `fit_scoring.py`: явная инструкция — при конфликте
   значений одного и того же параметра между источниками резюме (`HH_RESUME`/
   `ANKETA_FILE`) приоритетнее ответов (`ANSWER`), ответы приоритетнее видео
   (`VIDEO`); при отсутствии значения в приоритетном источнике — брать из следующего
   по приоритету без штрафа.
3. Явно закрепить в промпте: нехватка данных НЕ повод для искусственного занижения
   ниже того, что подтверждают присутствующие источники — оценка отражает реальный
   объём и качество доступных фактов, не блокирует кандидата статусом.
4. Проверить на 6 кандидатах, уже пересчитанных в E11-02 (`fit_score=75` до/после
   резюме) — вердикты не должны неожиданно разъехаться относительно того сравнения
   без причины (сравнить summary/strengths/risks на предмет использования резюме
   вместо анкеты при конфликте).

## Файлы

- `src/sfera_ai/services/fit_scoring.py` — `_SYSTEM_PROMPT`, `PROMPT_VERSION`
  (сменить версию промпта, т.к. поведение меняется — по прецеденту версионирования
  других промптов в проекте)

## Критерии готовности (DoD)

- [x] `_SYSTEM_PROMPT` содержит явную инструкцию приоритета резюме > ответы > видео
      при конфликте значений одного параметра
- [x] `_SYSTEM_PROMPT` явно говорит: нехватка данных не блокирует кандидата, оценка
      отражает объём доступных фактов
- [x] `PROMPT_VERSION` инкрементирован (была `fit-scoring-v1`, стала `fit-scoring-v2`)
- [x] `uv run pytest tests/services/test_fit_scoring.py` зелёный, регрессий нет
- [x] Ручная проверка на тех же 6 кандидатах E11-02 — вердикты объяснимы новым
      приоритетом, не случайный шум

## Как проверить

```bash
uv run pytest tests/services/test_fit_scoring.py -q
# + ручной прогон на 6 кандидатах course_id=39 (candidate_profile_id из E11-02),
#   сравнение summary/strengths с версией "было" из E11-02
```

## Как отметить выполнение

1. Впиши результат в журнал ниже. 2. Статус → `DONE`. 3. Обнови `04_STATE.md` (следующий шаг).

## Журнал

- `2026-09-02`: `_SYSTEM_PROMPT` дополнен инструкцией приоритета источников
  (резюме HH_RESUME/ANKETA_FILE > ANSWER > VIDEO при конфликте значения одного
  параметра; отсутствие значения в приоритетном источнике — брать из
  следующего без штрафа; нехватка данных не блокирует кандидата, отражается
  через `data_completeness`/`confidence`, не искусственным потолком
  `fit_score`). `PROMPT_VERSION` → `fit-scoring-v2`. Тесты
  `tests/services/test_fit_scoring.py` зелёные (6 passed), без правок тестов.

  Ручной прогон (владелец подтвердил) через `tunnel-platform-db.sh` +
  `tunnel-backend.sh`, `run_full_course_screening.py --course-id 39
  --candidate-ids 2301,2677,2760,3112,2378,3026` (тот же CLI-флаг из E11-02).
  Сравнение с версией «стало» из E11-02 (с резюме, старый промпт v1):

  | candidate_profile_id | было (E11-02, v1+резюме) | стало (v2) |
  |---|---|---|
  | 2301 | 70 POSSIBLE_MATCH | 75 POSSIBLE_MATCH |
  | 2677 | 75 POSSIBLE_MATCH | 75 POSSIBLE_MATCH |
  | 2760 | 65 POSSIBLE_MATCH | 75 POSSIBLE_MATCH |
  | 3112 | 65 POSSIBLE_MATCH | 75 POSSIBLE_MATCH |
  | 2378 | `NULL` (аномалия) POSSIBLE_MATCH | 70 POSSIBLE_MATCH |
  | 3026 | 75 POSSIBLE_MATCH | 70 POSSIBLE_MATCH |

  Все 6 остались `POSSIBLE_MATCH`, ни один вердикт не скакнул случайно.
  Побочно: известная аномалия `fit_score=NULL` у 2378 (зафиксирована в
  журнале E11-02, не расследовалась) в этом прогоне не повторилась —
  вернулся числовой `fit_score=70`; похоже на нестабильность LLM-ответа
  на прошлом прогоне, не системный баг промпта v1/v2 (не расследовалось
  глубже — не в scope этого шага). Тунели остановлены штатно после
  прогона, `jobs -l` пуст, порты 5433/8001 закрыты.
