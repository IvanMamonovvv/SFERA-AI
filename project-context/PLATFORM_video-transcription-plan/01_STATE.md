# STATE — транскрибация и summary видеовизиток

> Доска статуса. После каждого шага — обновить статус здесь + журнал в файле шага (не стирать историю).

**Общий статус:** 🟡 РЕАЛИЗАЦИЯ НАЧАТА (шаги 00-02 пройдены).
**Создан:** 2026-07-08.
**Согласованный стек:** self-hosted `faster-whisper` в воркер-контейнере + LLM (GigaChat/прокси) для summary; очередь в БД; env-рубильник на cloud.

## Доска шагов

| Шаг | Статус | Блокеры / примечание |
|---|---|---|
| 00 — спайк: бенчмарк сервера | ✅ DONE (2026-08-27) | `small`, `concurrency=2`, отдельный воркер-контейнер. Детали — журнал step-файла |
| 01 — модель `TranscriptionJob` | ✅ DONE (2026-08-27) | влито в main, раскатано на прод, таблица подтверждена reflection |
| 02 — постановка в очередь при завершении | ✅ DONE (2026-09-07) | перезапись видео не реализована (недостижима через API, см. журнал) |
| 03 — TranscriptionProvider (whisper) | 🟡 код готов (2026-09-07) | живой прогон с моделью — на 09 |
| 04 — SummaryProvider (LLM) | ⬜ TODO | после выбора LLM-провайдера |
| 05 — worker-команда | ⬜ TODO | после 01,03,04 |
| 06 — API карточки кандидата | ⬜ TODO | после 01 |
| 07 — HR UI | ⬜ TODO | после 06 |
| 08 — retention guard (30 дней) | ⬜ TODO | после 05 |
| 09 — тесты/smoke | ✅ DONE (2026-09-09, автотесты) | ручной smoke заведён в `06_TEST_CHECKLIST.md`, не пройден живым видео |
| 10 — prod rollout | ⬜ TODO | последний |

**VPS известен и недогружен:** Cloud MSK 80 — 4×3.3 ГГц · 8 ГБ RAM · 80 ГБ NVMe · Ubuntu 24.04.
Мониторинг за месяц: CPU < 20% (простаивает), реально занятой RAM ~2 ГБ (свободно ~6 ГБ), диск 27/80 ГБ.
**Риск нагрузки снят.** Параметры: `WHISPER_MODEL_SIZE=small`, `TRANSCRIBE_CONCURRENCY=2` (можно 3).

## Открытые вопросы (решить в ходе спайка)

- [x] Качество `small` на русском на реальном видео — норм, читаемый связный текст (owner
      решил не тестировать `medium`, `small` достаточно).
- [x] Реальное время обработки видео → concurrency=2 подтверждён (см. журнал step-00).
- [x] LLM для summary — **решено (2026-09-07): GPT-4o-mini через прокси** (proxyapi/vsegpt).
      Заодно упрощён скоуп step-04 — только транскрибация + краткий пересказ содержания,
      без классификации/вердикта (TARGET/NON_TARGET/NO_DATA убраны из плана).
- [x] Отдельный воркер-контейнер (не существующий `scheduler`) — решено на шаге 00.

## Журнал

- `2026-09-09` — шаг 09 (тесты и smoke) реализован в `sfera_backend`: новый
  `testchecks/tests/test_transcription_worker.py` (11 тестов) закрывает единственный
  реально непокрытый DoD-кусок — сам воркер (`claim_batch`/`reap_stale_jobs`/
  `_process_job`). Остальные автотест-пункты уже были покрыты шагами 02/06/08. Полный
  `manage.py test` — 657 passed, 3 skipped. Ручной smoke заведён в
  `project-context/06_TEST_CHECKLIST.md` (раздел «Видео-транскрибация»), не пройден —
  нет живого видео/запущенного воркера в этой сессии. Детали — журнал
  `step-09-tests-smoke.md`.
- `2026-09-07` — шаг 03 (TranscriptionProvider) реализован в `sfera_backend`:
  `testchecks/services/transcription/base.py` — Protocol + `LocalFasterWhisperProvider`
  (faster-whisper, модель-синглтон, `language=ru`, `WHISPER_MODEL_SIZE=small` из env),
  извлечение аудио через ffmpeg (паттерн `lessons/services/video_processing.py`),
  env-рубильник `TRANSCRIBE_PROVIDER` (только `local` реализован, `yandex`/`proxy` —
  `NotImplementedError`, вне объёма шага). `ruff check` чисто. faster-whisper НЕ
  установлен в `.venv` (тяжёлая зависимость с torch) — живой прогон на тестовом
  русском видео перенесён на step-09-tests-smoke.md (решение владельца). Не влито в
  git (изменения только в рабочей копии, коммит не делался). Детали — журнал
  `step-03-transcription-provider.md`.
- `2026-09-07` — шаг 02 (постановка в очередь при загрузке видеовизитки) выполнен и
  влит в `main`/`develop` (`sfera_backend`). Детали — журнал `step-02-enqueue-on-
  completion.md`. Следующий шаг — 03 (TranscriptionProvider) или 04 (SummaryProvider),
  независимы друг от друга по `EXECUTION_PLAN.md`.
- `2026-09-07` — владелец решил открытый вопрос по LLM: **GPT-4o-mini через прокси**
  (`SUMMARY_PROVIDER=proxy`). Также упростил скоуп step-04 — нужна только транскрибация
  + краткое понимание содержания речи кандидата, без классификации/вердикта. Правки
  внесены в `step-04-summary-provider.md` (+ снят `verdict` из `step-06`/`step-07`/
  `step-09`), `step-10-prod-rollout.md` (env). Заведён эпик `E14` в `SFERA-AI`
  (`05_EPICS.md`) — трекинг реализации в `sfera_backend` со стороны SFERA-AI, порядок
  выполнения в `EXECUTION_PLAN.md`. Реализация ещё не начата.
- `2026-08-27` — шаг 01 (модель `TranscriptionJob`) выполнен и раскатан: модель в
  `testchecks/models.py`, миграция `0009_transcriptionjob.py`, влито в `main` (PR #94)
  и применено на проде. Таблица подтверждена read-only через reflection из SFERA-AI.
  Django-admin в проекте не используется — пункт плана про регистрацию в admin неактуален.
  Детали — журнал `step-01-transcription-job-model.md`. Следующий шаг плана — 02
  (постановка в очередь при завершении видеовизитки), не срочно для SFERA-AI E4-02.
- `2026-08-27` — шаг 00 (спайк) выполнен. Реальный бенчмарк на VPS: `small`, `concurrency=2`,
  ~99 видео/час пропускная способность (экстраполяция на 3-мин видео), RAM некритична (~1.6ГБ
  на 2 параллельных процесса из доступных 3.2ГБ). Найдено: реальный available RAM на VPS теснее
  прежнего мониторинга (3.2Гб vs заявленных ~6Гб), диск теснее (16Гб своб. vs 53Гб), и сервер
  делит ресурсы с чужим `screening_asr` сервисом другого проекта — не учтено в исходном плане,
  риска не создаёт, но взято на заметку. Детали — журнал `step-00-spike-server-benchmark.md`.
  Следующий шаг — 01 (модель `TranscriptionJob` + миграция).
- 2026-07-08 — план создан (00_README, 02_CONTEXT, step 00–10). Реализация ожидает согласования и результатов спайка.
