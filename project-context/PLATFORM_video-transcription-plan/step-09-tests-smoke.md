# Step 09 — Тесты и smoke

**Статус:** ⬜ TODO
**Зависит от:** реализация step-01…07.
**Цель:** покрыть очередь, воркер и крайние случаи (особенно «пустое видео»).

## Юнит / интеграция (backend)

- [ ] Завершение анкеты с видео → создан ровно один `TranscriptionJob(PENDING)` (step-02, идемпотентность).
- [ ] Завершение без видео → джоб не создан.
- [ ] `claim_batch` + `skip_locked`: два «воркера» не берут один джоб (замокать/симулировать).
- [ ] Провайдер (замоканный) → джоб DONE, поля заполнены.
- [ ] **Пустое/тихое видео** → `is_empty=True`, `verdict=NO_DATA`, LLM не вызывается, summary осмысленный.
- [ ] Сбой провайдера → attempts++, retry, после MAX → FAILED, воркер не падает.
- [ ] Reaper возвращает зависший PROCESSING → PENDING.
- [ ] `clean_expired_videos` не трогает transcript/summary (step-08).
- [ ] API карточки кандидата отдаёт `transcription`; кандидат не получает (RBAC).

## Smoke (ручной, добавить в `06_TEST_CHECKLIST.md`)

- [ ] Кандидат записал нормальную визитку → через 1–5 мин у HR summary + вердикт + транскрипт.
- [ ] Кандидат записал 1 сек тишины / закрыл камеру → у HR «ничего не сказал / нецелевой», не пусто.
- [ ] 5 кандидатов завершили почти одновременно → все 5 обработались, платформа не тормозила.
- [ ] Перезапуск воркера в середине обработки → джоб доводится (reaper), не теряется.

## Критерий готовности

- [ ] Тесты зелёные, кейс «пустое видео» покрыт явно.
- [ ] Пункты добавлены в `project-context/06_TEST_CHECKLIST.md`.

## Журнал
- (пусто)
